package StorageLocations;



import org.testng.Assert;

import tests.BaseTest;

public class StorageLocationAssertions {
	StorageLocation page =new  StorageLocation();

	/*public StorageLocationAssertions verifyDashboard() throws InterruptedException {
		BaseTest.waitVisibilityOf(page.dashboardText);
		Assert.assertTrue(page.dashboardText.isDisplayed());
		return this;
		
	}
*/
	
}
